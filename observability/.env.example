# Grafana Cloud → Home → Stack name (slug, not display name)
STACK_NAME=your-stack-name
# Grafana Cloud API key with MetricsPublisher + LogsPublisher roles
ACCESS_TOKEN=glc_...
# Token for Alloy to auth with Home Assistant and gather metrics
HASS_METRICS_TOKEN
